const score = 400
console.log(score);

const balance = new Number(100)
console.log(balance);

console.log(balance.toString().length);
console.log(balance.toFixed(2));

// Prefer MDN Docs for more

// ********************** Maths ********************

console.log(Math); // Math --> Object (check in browser)

// Prefer MDN Docs for more