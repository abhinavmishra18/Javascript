const name = "Abhinav"
const repoCount = 50

console.log(name + repoCount + " Value");

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

const gameName = new String('Abhinav')

console.log(gameName[0]); // This is a key and will Access a value that will be 'h'

// Prefer MDN Docs for String methods

